<div class="container mr-md-3 mt-3" >
<h1>Articles</h1>

<p class="d-flex flex-row justify-content-start" just>
  <% @articles.each do |article| %>
  <%=link_to article.title, article_path(article)%> <%=article.content%>
  <% end %>

<div  class="container m-16">
<h2><%= link_to "Create new Article", new_path(@article)%></h2>
</div>

</div>
